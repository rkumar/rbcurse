# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/rbcurse.rb'

Hoe.new('rbcurse', Rbcurse::VERSION) do |p|
  # p.rubyforge_name = 'rbcursex' # if different than lowercase project name
   p.developer('rkumar', 'FIX@example.com')
end

# vim: syntax=Ruby
